# Streamlit Vega-Lite

## Dev Setup

Open two temrminals int the dev container. In the first one, run `pip install -e .` and then `streamlit run streamlit-vega-lite/__init__.py`. In the second one, run `yarn` and `yarn start`. Then open http://localhost:8501/.
